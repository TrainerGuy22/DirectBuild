distributions {
    main {
        baseName = 'SimpleCI'
        contents {
            from("${buildDir}/libs/SimpleCI.jar") {
                into("lib")
            }
        }
    }
}

tasks.getByName("distZip").dependsOn("jar")
tasks.getByName("distTar").dependsOn("jar")

task dist(dependsOn: [ "distZip", "distTar" ]) {}